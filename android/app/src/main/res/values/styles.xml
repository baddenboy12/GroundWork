<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- windowBackground is what shows behind the now-transparent system
             bars (post-EdgeToEdge.enable in MainActivity); must be dark to
             match the rest of the app. android:background is a no-op for
             activities and used to be set to @null — left out now. -->
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:fitsSystemWindows">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- DO NOT use @drawable/splash here. That is the default Capacitor
             template (blue X on white) and on some devices (e.g. OnePlus 13)
             it stays visible as the activity's window background after
             launch, showing through transparent system bars and the
             WebView's inset-padding region as a white band.

             The activity stays on this theme post-launch in Capacitor's
             setup, so the windowBackground needs to be a flat dark color
             matching the rest of the app. The Capacitor SplashScreen
             plugin handles splash display at runtime (config has
             launchShowDuration: 0, so the splash isn't actually shown). -->
        <item name="android:windowBackground">@color/app_background</item>
        <item name="windowSplashScreenBackground">@color/app_background</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:fitsSystemWindows">false</item>
        <!-- Suppress the native ActionBar / title. Some OEMs (OnePlus) show
             the activity's android:label as a title bar above the WebView
             unless these are explicitly disabled — Theme.SplashScreen's
             defaults differ from Theme.AppCompat.NoActionBar. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>