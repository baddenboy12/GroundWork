/**
 * App version string — bump this manually before publishing a new release
 * so you can confirm which build the PWA / browser is running.
 *
 * Convention: MAJOR.MINOR.PATCH
 *   MAJOR — breaking schema changes or complete redesigns
 *   MINOR — new features / milestones
 *   PATCH — bug fixes
 */
export const APP_VERSION = "241";
